import * as React from 'react';
import * as ReactDOM from 'react-dom/client';
import LoginForm from './loginForm';

ReactDOM.createRoot(document.getElementById('login-form-root')).render(<LoginForm />);